document.addEventListener("DOMContentLoaded", (event) => {
  const button = document.getElementById("welcomeButton");
  button.addEventListener("click", () => {
    alert("¡Bienvenido a la Biblioteca!");
  });
});
